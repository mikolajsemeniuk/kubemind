---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: asks.mind.kubemind.io
spec:
  group: mind.kubemind.io
  names:
    kind: Ask
    listKind: AskList
    plural: asks
    singular: ask
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Ask is the Schema for the asks API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: AskSpec defines the desired state of Ask.
            properties:
              contextScope:
                description: ContextScope limits where knowledge is searched (future
                  use).
                type: string
              question:
                description: Question asked to KubeMind.
                type: string
            type: object
          status:
            description: AskStatus defines the observed state of Ask.
            properties:
              answer:
                type: string
              answeredAt:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
